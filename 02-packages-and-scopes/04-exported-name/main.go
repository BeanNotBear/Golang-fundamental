package main

import (
	"fmt"
	"math"
)

func main() {
	// in golang an exported name begin with a capital letter
	// ex: math.Pi (P is capital letter)
	// in this example Pi is an exported name
	fmt.Println(math.Pi)
}
